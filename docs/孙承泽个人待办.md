# 孙承泽 · 可视化个人待办清单

> 角色定位：**我只负责可视化模块**，不是组长。下面的清单只针对我本人，不要求我向全组交付。
> 原则（来自假期前 PPT）：学习顺序 = **先能跑 → 再准确 → 再高级**；各组数据/模型未到位前，我用 FLORIS 占位，接口固化后**零改动替换**。
> 可视化交付物已落地：`site/`（纯静态，可一键部署 Cloudflare Pages，本地预览已起）。

---

## 0. 我的定位与替换策略（最重要）

- 我吃的是 **FLORIS 工程尾流模型的占位数据**，喂给演示系统看。
- 真实链路应是：**田铭雨(CFD) → 袁夫达(NN训练) → 洪祖名(优化) → 我(可视化)**。
- 我预先固化了**统一数据接口**，把"格式错配"风险降到最低。各组给真实东西时，**只替换函数体/数据文件，界面 0 改动**。
- 当前没有大卡点；若各组暂未提供，我继续用 FLORIS 占位即可。

---

## 1. 今日交付清单（2026-08-08）

- [x] **把 Streamlit 重写为纯静态站点**：`site/`（HTML + 原生 SVG 图表 + 浏览器内插值），零依赖、离线可用、可部署 Cloudflare Pages。本地预览已起。
- [x] **固化统一数据接口契约**：`predict_power(yaw, U_inf)→(P1,P2) kW`、`find_yaw_for_target(target, U_inf)→(yaw, power, err%)`；数据文件替换计划写进站点"统一数据接口"页。
- [ ] **推到 GitHub + 部署 Cloudflare Pages**，把网址发给组内看（见 `site/README.md` 部署步骤）。
- [ ] 自检：各页数字与 `optimizer_result.json` / `array_independent_result.json` 一致（已验证 yaw 25°@8m/s → 2368 kW，对得上）。

---

## 2. 按学习顺序的三阶段个人任务

### 阶段一 · 先能跑（基础骨架，已基本完成）
- [x] 功率–偏航曲线、偏航寻优、3×3 阵列优化、风玫瑰、流场图、模型精度页。
- [x] 统一数据接口页（让组内一眼看懂"怎么接我的东西"）。

### 阶段二 · 再准确（等各组真实数据/模型到位后）
- [ ] 用**厉今飞/洪祖名真实模型**替换 `predict_power`：加载 `final_models/*.json`（XGBoost）或 keras 模型，界面不动。
- [ ] 用**洪祖名真实优化器结果**覆盖 `optimizer_result.json` / `array_independent_result.json`，界面数字自动更新（以洪的为准）。
- [ ] 用**田铭雨真实 CFD 流场**替换 `fields/` 图片源，界面不动。
- [ ] 复算并核对：我的展示数字与各组口径一致（尤其是偏航增益、全场功率）。

### 阶段三 · 再高级（'在全国突出'的关键，借鉴本科生答辩材料 §3.9）
- [ ] **实时仪表盘风格**：半圆三色仪表（绿-黄-红）、功率滚动曲线、阈值告警——把"丝滑"和"专业感"做出来。
- [ ] **偏航扫描动画升级**：GIF → MP4 / Canvas 丝滑播放（当前 wake_animation.gif / array_animation.gif 先顶着）。
- [ ] **交互联动**：拖拽偏航角，全场功率与流场图实时联动更新。
- [ ] **零依赖极速站**：纯静态 + CDN 缓存，加载快、不卡——这是相对 Streamlit 的最大体验优势。

---

## 3. 需要各组给我的东西（明确清单，便于催）

| 来自 | 我要什么 | 替换到哪 | 当前占位 |
| --- | --- | --- | --- |
| 袁夫达 / 厉今飞 | 真实功率代理模型文件 | `predict_power` 函数体 | FLORIS 插值 |
| 洪祖名 | 真实优化器结果 JSON | `optimizer_result.json` / `array_independent_result.json` | 我的 FLORIS 结果 |
| 田铭雨 | 高保真(CFD)流场图/数据 | `fields/` 图片源 | FLORIS 工程尾流 |

> 注：洪祖名已给出"代理模型优化交付"（XGBoost 选定，全场 MAE 7.79/10.49 kW），模型文件在 `20260808/代理模型优化交付_20260808.zip → final_models/`；阶段二第一项可直接接它。

---

## 4. 给你自己的提醒
- 不要替别人写模型/优化/数据，只把自己这块做"丝滑 + 可交互 + 易替换"。
- 每次替换真实数据后，跑一遍 `site/build_data.py` 重新导出 `data.js`，界面即更新。
- 答辩/展示时强调：**接口契约清晰、实时可交互、零依赖快站**——这三点最容易在全国层面脱颖而出。
