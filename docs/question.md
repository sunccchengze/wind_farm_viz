# question.md — 致孙承泽（可视化负责）

> 以李良星老师的身份写给孙承泽的 직접提问。  
> 没有「合理推测」——有疑惑就 직접问，看你能回答多少，答不了的就标记出来，回头当面对齐。

---

## 一、关于当前可视化系统和数据链路

### Q1
你现在的Streamlit系统里，`surrogate_model.py`用的是scipy的`RegularGridInterpolator`对FLORIS生成的`cases_multi.csv`做线性插值。这个插值模型究竟是**为了演示而写的过渡方案**，还是已经作为正式接口在用？应用场景是仅仅在Streamlit页面里画图，还是会被优化求解器（`find_optimal_yaw`、`find_yaw_for_target`）当作正式的预测函数来调用？

### Q2
`surrogate_model.py`里的`predict_power_2d`函数里有这一段：

```python
cos_factor = np.cos(np.radians(y2)) ** 1.5
p2 = p2_0 * cos_factor
```

这是对下游风机偏航后功率衰减的经验修正。这个公式是从哪里来的？FLORIS里有没有对应的物理模型？还是你自己凭经验加的？如果是经验公式，**它的合理性和适用范围有没有经过验证**？偏航角到±30°之外还能用吗？

### Q3
README里写得很漂亮：「未来替换 PINN 后界面零改动」。这个说法本身没问题，但我想确认：现在的`cases_multi.csv`里只有**两台风机**的功率数据（`power_1`和`power_2`）。如果将来神经网络模型输出的是**三台风机功率**（如厉今飞训练的模型那样），或者输出的是整个流场的速度场，你的可视化页面的输入接口需要做哪些实质性的改动？「零改动」具体指哪些部分零改动？

### Q4
`dashboard.py`（019fcc85分支上新加的）里显示「数据源：FLORIS / 实验室模拟」。这里的「实验室模拟」指的是什么？田组是不是已经有了实验室测量数据？如果有，是什么类型的（风洞？桌面风机？），规模多大，格式是什么？

---

## 二、关于神经网络代理模型（厉今飞的工作）——请你转达/整理后回答

### Q5
厉今飞训练的神经网络，输入是 `wind_speed, wind_direction, turbulence_intensity` → 编码成6个特征 → 输出3个风机功率比例 + 总功率。**这个模型没有偏航角作为输入**。也就是说，它预测的是**偏航角=0°（对齐风向）**时的功率。如果我们要用这个模型来做偏航优化，必须要能预测「给定偏航角下的功率」。请问厉今飞的模型是否已经覆盖了偏航角维度？如果没有，下一步是要让模型输入包含偏航角，还是要另外训练一个偏航敏感的模型？

### Q6
神经网络输出层用的是Sigmoid（输出限制在0~1），最终乘以额定功率（5MW/15MW）得到实际功率。这样做有一个隐含假设：**任何工况下风机功率都不会超过额定功率**。而在FLORIS的功率曲线里，风速超过额定风速（NREL 5MW大约是11.4 m/s）后功率会平坦在5MW。这已经被模型的输入范围（最大12 m/s）覆盖了吗？如果风速稍微超过一点，模型预测会不会异常？

### Q7
训练数据里有1224条样本，神经网络有约17,000个参数。**参数/样本比是20:1**。常规建议是这个比值应当小于10，最好小于5。你怎么看这个过拟合风险？虽然训练曲线显示验证损失也在下降，但FLORIS数据的光滑性可能会掩盖真实的过拟合。**有没有做过交叉验证？有没有尝试过更小的网络（比如只有1个隐藏层）作为baseline？**

### Q8
激活函数用的是Swish。选择Swish而不是ReLU、GELU或Tanh的理由是什么？有没有做过消融实验（ablation study）？损失函数用的是MSE。对于功率预测这种目标变量有明确物理范围（0~额定功率）的问题，MSE是否是最合适的损失函数？有没有考虑过加权MSE（比如对低功率区误差给予更高权重，因为低功率区的相对误差往往更大）？

---

## 三、关于数据（袁夫达的工作）——请你协调后回答

### Q9
袁夫达说：「数据不是直接在github仓库上摘取，而是调用了floris中的两种基本内置函数模型（GCH尾流模型和CC尾流模型），基于多种风况生成对应的风机功率数据。」这里的「调用」是通过FLORIS的Python API批量运行吗？生成脚本在哪里？（`floris(1).zip`里好像没有数据生成脚本，只有训练脚本。）

### Q10
GCH模型和CC模型预测的功率有差异。从元数据看，GCH模型生成的训练数据里，风机功率的最小值更高（turbine_1最小56340W vs CC的23107W）。这说明**GCH模型预测的尾流损失更小**。请问袁夫达：这两种模型的差异在什么物理机制上？哪种更接近真实情况？未来要用哪种作为训练基准？如果将来要引入Fluent高保真数据，GCH和CC的差异对神经网络训练的意义是什么？

### Q11
「在fluent跑出高保真数据之前，我们可以尝试通过优化函数来提高数据精度作为训练素材。这也是后续可能的推进方向。」（袁夫达）——这里的「优化函数」具体指什么？是优化FLORIS的参数（比如湍流参数、尾流松弛因子）以匹配某种基准吗？还是改良尾流模型本身的算法？**这个方向的具体技术路线是什么？**

---

## 四、关于仿真数据（田组）——请你直接问田

### Q12
README里定义了`fields/case_XXXX.npz`的格式（128×64网格，变量x/y/u）。目前`fields/`目录下有12个case（case_0001~case_0012）。这些数据是用FLORIS生成的还是用CFD生成的？如果是FLORIS，**为什么需要仿真组单独生成，而AI组的FLORIS数据又是怎么生成的？两者是同一次仿真吗？** 如果仿真组将来要提供CFD数据，网格格式、变量定义是否要重新约定？

### Q13
你现在可视化用的`fields_3d/yaw_+00.npz`等5个三维文件，是谁生成的？格式是(9, 32, 64) —— 9个高度层 × 32个y坐标 × 64个x坐标。高度范围是20~180m。这个数据是从FLORIS导出的吗？还是田组用其他工具生成的？

---

## 五、关于优化控制（洪组）——请你直接问洪

### Q14
`optimizer_result.json`里目前只有一组结果（风速8m/s，偏航角+25°，功率提升8.13%）。这个结果是基于什么模型算的？FLORIS直接优化？还是基于插值代理模型搜索的？洪组的优化算法准备用什么方法？网格搜索、梯度下降、遗传算法、强化学习？**这个优化的最终目标是实时控制（需要毫秒级响应）还是离线优化（可以用较慢的方法）？**

### Q15
功率需求跟踪（`10_power_tracking.py`）的接口已经定义好了。但当前的实现（`surrogate_model.py`的`find_yaw_for_target`）是基于两台风机的2D网格搜索（yaw1 × yaw2，60×60=3600个点）。**如果扩展到3台风机甚至更多，网格搜索的复杂度会指数增长。洪组有没有考虑过更高效的优化方法？是否要等神经网络模型ready后再做优化算法的选型？**

---

## 六、关于项目整体推进——老师建议你思考并直接回答

### Q16
申请书里的技术路线是：**数据驱动分析 → PINN → 基于PINN的启发式优化算法**。目前团队的工作处在第一个环节的早期阶段（纯数据驱动的回归模型）。请你梳理一下：从「1224条FLORIS数据上的神经网络回归」到「能够指导实际风电场运行的智能调控系统」，中间还有哪些关键里程碑？大概的时间安排是什么？哪些是大创结题时必须完成的？

### Q17
团队里目前有5个人：厉今飞（AI）、袁夫达（建模）、田铭雨（仿真）、洪祖名（优化控制）、孙承泽（可视化）。请你用一段话描述一下**目前各组之间的协作关系和数据流转情况**。比如：袁夫达生成数据 → 厉今飞训练模型 → 洪组用模型做优化 → 田组提供仿真验证 → 你把结果可视化。这条链路现在走到哪一步了？ bottleneck 在哪里？

### Q18
李良星老师在指导意见里反复强调：「所有的工作，首先要面向研究目标；然后针对每一步工作和方法，要说明选择该方法的原因及合理性；针对结果要给出分析和讨论；最后思考接下来优化改进的方向」。请你回答：**目前团队的工作中，哪一部分是「有明确的研究目标支撑」的，哪一部分是「还没有理清为什么要这么做」的？**

---

## 七、需要尽快澄清的技术细节

### Q19
`floris(1).zip`里的两个模型（CC和GCH），训练脚本完全一样，唯一区别是读入的CSV文件不同。请问：**为什么要同时训练两个模型？** 是为了比较哪个尾流模型更适合作为神经网络的训练基准，还是为了构建某种集成（ensemble）？

### Q20
从元数据看，两种模型的训练集样本数略有不同：CC是854条训练样本（总1224条），GCH是840条训练样本（总1224条）。这意味着两种模型**剔除的零功率样本数不同**（CC剔除了更多）。请问：为什么两个模型生成的数据里零功率样本数不同？是尾流模型的差异导致的，还是数据生成时的随机性？

---

*孙承泽，以上问题不用一次性回答。你可以先回答你掌握的部分，没把握的就标记为「不确定，需要当面问X」。我会根据你的回答继续追问。*
