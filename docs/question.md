# 李良星老师对孙承泽的 20 个问题

> 角色代入：指导老师 **李良星** → 学生 **孙承泽（可视化组）**。
> 原则（老师原话）：所有工作首先面向研究目标；每步方法要说明选择与合理性；结果要给分析讨论；最后思考优化改进方向。**不要藏着掖着，任何不确定都问。**
> 说明：本文件先基于仓库已有材料（代码 + `floris(1).zip` + 申请书）整理；2026-08-08 群聊消化后可能补充（见 `chat_20260808_digest.md`）。

---

## 一、关于可视化系统和数据链路（Q1–Q4）

**Q1.** `surrogate_model.py` 里用 `RegularGridInterpolator` 的插值模型，是给演示系统**临时过渡**，还是打算作为正式代理模型长期保留？将来若换成厉今飞的神经网络模型，接口怎么兼容（输入维度、输出单位）？

**Q2.** `predict_power_2d` 里 `cos_factor = cos(yaw2)**1.5` 这个 **1.5 次方**是哪来的？经验拟合、文献出处，还是拍脑袋？有没有和 FLORIS 真值比对过误差？

**Q3.** README 里写的“PINN 零改动”具体指什么？是说可视化层完全不碰物理模型，还是说现阶段压根还没接 PINN？

**Q4.** `app.py` 里展示的偏航优化增益（+14.9% 阵列、+8.1% 双机）数据来源是洪祖名的 `optimizer_result.json` / `array_independent_result.json`，还是你自己另跑的？两处数字若对不上，以谁为准？

---

## 二、关于神经网络模型（Q5–Q8）

**Q5.** 厉今飞的 NN **没有偏航角（yaw）作为输入**，只有风速 / 风向 / 湍流 / 坐标。那“偏航优化”要怎么用这个模型做？偏航影响是被淹没在“风向”里了，还是说这个模型只能用于固定布局？

**Q6.** CC/GCH 训练后有效样本约 840–854 条，参数量约 1.7 万，**参数/样本比≈20:1**。以老师经验，这种比在小样本上很容易过拟合，`EarlyStopping + L2` 够不够？要不要做 k 折或贝叶斯优化？

**Q7.** 隐藏层用 **Swish**、输出层用 **Sigmoid**，理由分别是什么？Sigmoid 把功率压到 [0,1] 再乘额定值——为什么不用 ReLU/线性输出 + 裁剪？Sigmoid 在接近 5MW 的饱和区会不会削掉真实高功率？

**Q8.** 输出层那个**固定求和层**（三机功率求和为全场）是硬约束，这算不算一种最朴素的物理融合？除了求和，还考虑过把尾流守恒、动量缺损写进损失函数吗？

---

## 三、关于数据（袁夫达工作，Q9–Q11）

**Q9.** 生成脚本（`train_wind_farm_model.py` 之外，真正的 FLORIS 数据生成脚本）在哪？仓库里只看到 `floris(1).zip` 里的训练脚本，原始 `input` 文件夹和批处理脚本能否也归档进来？

**Q10.** GCH 和 CC 两套数据训练出来指标略有差异（GCH 的 R² 略高、MAE 略低），这背后的**物理意义**是什么？我们到底该信哪套，还是两套怎么配合使用？

**Q11.** 袁夫达说“在 Fluent 高保真数据之前，可先**优化函数提高数据精度**”。这个“优化函数”具体指哪条技术路线？调 FLORIS 模型参数、换更高阶尾流模型，还是做误差校正网络？

---

## 四、关于仿真数据（田铭雨工作，Q12–Q13）

**Q12.** `fields/*.npz`、`fields_3d`、`fields_array` 这些流场数据，到底是 **FLORIS 工程尾流模型**输出的，还是 **Fluent/CFD 高保真**跑的？申请书里写的是高保真，现在到底到哪一步了？

**Q13.** 3D 流场 `fields_3d/yaw_*.npz` 是田组用 `generate_3d_data.py` 生成的？`pod_analysis.py` 的 POD 分解目的是**降维做流场代理**，还是为 **PINN 提供模态基**？

---

## 五、关于优化控制（洪祖名工作，Q14–Q15）

**Q14.** `optimizer_result.json` 里推荐的偏航角是**离线算好写死**的，还是运行时**实时优化**？将来接电网需求（`find_yaw_for_target`）时，优化器是在线求解还是查表？

**Q15.** 现在是 2 机 / 9 机（3×3）阵列，怎么扩展到申请书里更大的风场？**独立偏航（independent, +24%）** 和 **统一偏航（unified, +14.87%）** 的取舍，工程上怎么落地？

---

## 六、关于项目整体（Q16–Q18）

**Q16.** 从“FLORIS 数据 → 代理模型 → 可视化 → 阵列优化 → PINN → 智能调控”，还有哪些关键里程碑没列出来？时间线到 2027 年 6 月，按现在进度到哪个节点了？

**Q17.** 各组现在的协作链路走到哪了？可视化（你）吃的是谁的产出、喂给谁看？中间有没有“数据格式对不上、得手动改”的卡点？

**Q18.** 申请书里“数据驱动 + PINN + 启发式优化”三件套，目前只有数据驱动（NN/插值）和一点点优化，**PINN 连影子都没有**。这条线谁牵头、什么时候启动？

---

## 七、技术细节（Q19–Q20）

**Q19.** 为什么 GCH 和 CC **两个模型都训练**？为了对比选优，还是申请书/结题要两套？维护两套模型的成本考虑过吗？

**Q20.** 零功率样本剔除：CC 剔完剩 854、GCH 剩 840，**差 14 条**。为什么两套数据的零功率行数不同？是风向/湍流组合导致的边界工况差异，还是生成脚本随机性？这个差异会影响“两套模型公平对比”吗？
