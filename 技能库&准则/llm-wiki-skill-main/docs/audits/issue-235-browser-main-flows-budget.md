# #235 browser-main-flows 作业预算证据

## 完成与验证标准

- 依赖、系统准备、浏览器下载与安装、浏览器启动验证、正式测试和诊断上传各自可观察，并有独立失败边界。
- 冷缓存会完整准备环境；缓存命中只省略浏览器下载与安装，不能省略系统依赖或浏览器启动验证。
- 安装失败或不退出时，相关进程在阶段时限内结束，退出状态和脱敏输出进入诊断材料。
- 正式测试失败后，runner 完成清理并生成脱敏证据，作业仍有时间执行上传。
- 不增加固定休眠、自动重试、选择性重跑、跳过正式测试或放宽产品断言。
- PR 最新提交的 `quality-and-tests` 与 `browser-main-flows` 必须真实成功；合并后的 `main` 复验留给合并收尾。

## 历史运行证据

| 运行 | npm 依赖 | 系统依赖 | 浏览器下载与安装 | 正式测试 | 结果 |
| --- | ---: | ---: | ---: | ---: | --- |
| PR #228，run `29248599663` | 9 秒 | 与浏览器合计 36 秒 | 与系统合计 36 秒 | 29 秒 | 成功 |
| PR #228 合并后的 main，run `29248894869` | 11 秒 | 7 分 51 秒后随作业取消 | 未开始 | 未开始 | 取消，上传跳过 |
| PR #261 合并后的 main，run `29428721669` | 10 秒 | 约 23 秒 | 约 9 秒 | 45 秒 | 成功 |

失败运行的原始日志表明，`install --with-deps` 在系统字体包下载期间长时间停顿；浏览器的 177 MiB Chromium、2.3 MiB FFmpeg 和 114.2 MiB headless shell 下载均未开始。相邻成功运行又证明同一混合命令通常可在几十秒内完成，因此单次慢系统源不是完整根因。根本缺口是系统依赖、浏览器安装、正式测试和上传共用 8 分钟总时限，任何准备阶段停顿都能静默挤掉后两者。

## 阶段与缓存边界

| 阶段 | 发生位置 | 内部上限 | 工作流上限 | 可观察证据 |
| --- | --- | ---: | ---: | --- |
| npm 依赖 | `npm ci` | 90 秒 | 2 分钟 | 阶段 JSON、脱敏日志、退出码 |
| 缓存身份 | 锁文件、Playwright 包版本、浏览器清单 | 不适用 | 1 分钟 | 身份 JSON、步骤输出 |
| 缓存恢复 | `actions/cache` | 不适用 | 2 分钟 | GitHub 步骤结论与 `cache-hit` |
| 系统依赖 | `playwright install-deps chromium` | 8 分钟 | 9 分钟 | 阶段 JSON、脱敏日志、退出码 |
| 浏览器下载与安装 | `playwright install chromium`，仅缓存未命中 | 3 分钟 | 4 分钟 | 每个内容的下载开始、落盘完成时间点及阶段结果 |
| 浏览器验证 | 实际启动并关闭 Chromium | 45 秒 | 1 分钟 | 阶段 JSON、启动结果 |
| 正式测试 | 既有 browser-main-flows runner | 270 秒 | 5 分钟 | runner 输出、阶段 JSON、退出码 |
| 诊断上传 | `actions/upload-artifact` | 不适用 | 2 分钟 | GitHub 步骤与 artifact |

工作流步骤上限合计 28 分钟，作业上限 32 分钟，保留 4 分钟给步骤切换、runner 清理、Action 收尾和诊断处理。系统依赖的 8 分钟内部边界来自历史 7 分 51 秒停顿；它会在作业总时限前明确失败。浏览器安装和正式测试上限分别以成功运行约 9 秒下载与安装、最长 45 秒正式测试为基线，并保留数倍余量。此次本地代表性实测为：`npm ci` 8.873 秒，浏览器启动验证 0.821 秒，正式测试阶段 38.275 秒。

缓存目录为工作区内的 `.cache/ms-playwright`。精确键由 `runner.os`、`runner.arch`、锁文件中的 Playwright 版本，以及 Playwright 清单内 Chromium、headless shell 和 FFmpeg 的版本与 revision 共同决定；没有宽松恢复键。系统或架构变化、Playwright 锁定版本变化、实际浏览器内容变化都会未命中。缓存未命中执行完整安装；命中只跳过该安装，系统依赖与实际启动验证仍执行。

## 失败与诊断路径

| 场景 | 预期观察 |
| --- | --- |
| 冷缓存 | cache miss，浏览器下载与安装步骤运行并记录内容时间点，随后浏览器验证和正式测试成功 |
| 常规运行 | 精确 cache hit，浏览器安装步骤跳过，系统依赖、浏览器验证和正式测试仍成功 |
| 系统依赖失败 | 系统依赖步骤返回非零，记录退出码和脱敏日志，正式测试不启动，诊断上传执行 |
| 浏览器安装失败 | 浏览器安装步骤返回非零，记录已到达的下载时间点、退出码和脱敏日志，诊断上传执行 |
| 安装不退出 | 内部时限触发，子进程树被清理，状态为 `timed-out`，诊断上传执行 |
| 正式测试失败 | 既有 runner 先清理浏览器、前后台与端口并写入脱敏证据；外层阶段记录失败，上传同时包含 runner 与阶段证据 |
| 作业被整体取消 | GitHub 不再调度后续上传；32 分钟上限大于所有步骤上限之和，正常阶段失败不会先耗尽作业总时限 |

直接负向验证使用受控短时子进程和退出事件，不使用固定休眠，也不删除或覆盖任何 GitHub 缓存。外层阶段 runner 收到 `SIGTERM` 时会先结束活动进程树、写入 `interrupted` 记录并保留退出状态；正式测试 runner 原有失败、中断、超时清理与脱敏路径继续由共享进程执行器验证覆盖。

## 线上验收状态

PR 创建后的首次运行用于验证新键的受控冷缓存路径；随后以必要的证据更新触发同一 PR 的常规缓存命中路径。这里只记录真实完成的运行，不点击重跑。PR 合并前不能完成 #235 要求的 main push 复验，也不能提前移动后续任务标签。

### PR #262 冷缓存

- 提交：`dfa7005d403478dbfe8d8cb0f632b5d53d5d8ddf`
- `browser-main-flows`：run `29432351080`，成功，作业 1 分 53 秒。
- `quality-and-tests`：run `29432350305`，成功，作业 3 分 29 秒。
- 浏览器缓存：精确键 `Linux-X64-playwright-1.61.0-chromium-3196eaae49e97b56` 未命中。
- npm 依赖：11.977 秒；系统依赖：14.033 秒；浏览器下载与安装：11.128 秒；浏览器启动验证：0.784 秒；正式测试：53.918 秒。
- Chromium 在 0.533 秒开始下载、6.032 秒落盘；FFmpeg 在 6.032 秒开始、7.138 秒落盘；headless shell 在 7.138 秒开始、11.094 秒落盘。
- 正式测试实际开始并通过；诊断上传步骤成功，11 个脱敏文件进入 artifact `8349936301`。

### PR #262 常规缓存命中

- 提交：`c2be9cf39708f53643143020ebcb78a910cc5221`
- `browser-main-flows`：run `29432659852`，成功，作业 1 分 44 秒。
- `quality-and-tests`：run `29432659895`，成功，作业 3 分 8 秒。
- 浏览器缓存：精确键 `Linux-X64-playwright-1.61.0-chromium-3196eaae49e97b56` 命中；浏览器下载与安装步骤明确跳过。
- npm 依赖：8.089 秒；系统依赖：28.641 秒；浏览器启动验证：2.445 秒；正式测试：44.017 秒。
- 系统依赖和实际浏览器启动验证均未被缓存命中省略；正式测试与诊断上传继续成功。

PR 的冷缓存与常规路径均已在声明预算内进入并完成正式测试。当前仍不宣称 main push 验收完成；该项必须等 PR 合并后在合并提交上复验。

参考：[Playwright 浏览器与系统依赖安装](https://playwright.dev/docs/browsers)、[GitHub Actions dependency caching](https://docs.github.com/en/actions/reference/workflows-and-actions/dependency-caching)、[GitHub Actions workflow syntax](https://docs.github.com/en/actions/reference/workflows-and-actions/workflow-syntax)。
