# 视频背景

把整站背景视频命名为 `bg.mp4` 放到这个目录（建议 10–20MB 以内、1080p、静音循环，
可用 ffmpeg 压缩）。页面会自动加载并叠加蒙版保证文字可读。

- 没有该文件时，页面保持纯色背景，无任何影响。
- 想临时关闭：浏览器控制台执行 `localStorage.setItem('wf_bg','off')` 后刷新。
- 可选封面图 `bg.jpg`（视频加载前显示）。
