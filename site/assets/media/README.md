# 首页数字孪生视频

`bg.mp4` 由 `site/index.html` 与 `site/index_v2.html` 的“风力涡轮机数字孪生与线框结构运行监测视口”直接引用。

- 内容边界：仅表示风力发电机轮毂、齿轮箱、机舱和线框结构运行示意，不是 3×3 九机阵列尾流实录。
- 播放方式：HTML 原生 `autoplay muted loop playsinline`，无需网络请求、运行时探测脚本或本地存储开关。
- 部署要求：保持文件名 `bg.mp4`；若替换素材，必须同步核对页面说明、授权和事实边界。
